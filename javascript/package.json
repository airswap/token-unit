{
  "name": "token-unit",
  "version": "0.2.1",
  "description": "Convert between token units and ether units",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "compile": "babel --source-maps -d lib/ src/",
    "build": "npm run compile && gulp build && mocha",
    "release": "npm run compile && gulp build && mocha && npm publish build"
  },
  "author": "AirSwap.io",
  "license": "MIT",
  "dependencies": {
    "big.js": "^5.2.1",
    "lodash": "^4.17.11",
    "randomatic": "^3.1.0",
    "require-all": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-rename": "^1.2.2",
    "gulp-replace": "^0.6.1",
    "mocha": "^4.0.1"
  },
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/airswap/token-unit/tree/master/javascript"
  },
  "keywords": [
    "ethereum",
    "token",
    "conversion",
    "wei",
    "ether",
    "web3",
    "erc20",
    "token",
    "token"
  ]
}
